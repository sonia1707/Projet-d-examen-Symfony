{% extends 'base.html.twig' %}

{% block title %}Arret{% endblock %}

{% block body %}
    <h1>Arret</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ arret.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ arret.nom }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ arret.latitude }}</td>
            </tr>
            <tr>
                <th>Longitude</th>
                <td>{{ arret.longitude }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_arret_index') }}">back to list</a>

    <a href="{{ path('app_arret_edit', {'id': arret.id}) }}">edit</a>

    {{ include('arret/_delete_form.html.twig') }}
{% endblock %}
